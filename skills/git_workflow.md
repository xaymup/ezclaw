# Skill: git_workflow

## Description
A safe workflow for committing changes to a git repository.

## Procedure
1. Check status: `git status`
2. See changes: `git diff`
3. Add specific files: `git add <file>`
4. Commit with message: `git commit -m "Your message"`
5. Verify: `git status`
